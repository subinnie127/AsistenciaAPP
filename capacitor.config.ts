import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Registro_APP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
